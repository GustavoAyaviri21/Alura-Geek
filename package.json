{
  "name": "alurageek",
  "version": "1.0.0",
  "description": "challenge Alura Latam",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run live\"",
    "server": "json-server --watch db.json --port 5000",
    "live": "live-server --port=3000 --no-browser",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2"
  }
}
